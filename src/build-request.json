{
  "kind": "build_request",
  "title": "Replace accepted-view emoji spans with a repeated emoji sequence",
  "priority": "normal",
  "requirements": [
    {
      "id": "REQ-1",
      "text": "Update only the 5 selected <span> elements in the accepted view (the five bouncing emoji spans) so that each span’s text content becomes exactly: \"❤ 🥰 😏 💋💌\", while keeping all existing classes (including animate-bounce and delay classes), layout, and behavior unchanged.",
      "target": "frontend",
      "source": {
        "messageIds": [
          "user-1"
        ],
        "quotes": [
          "replace with ❤ 🥰 😏 💋💌",
          "3 yes"
        ]
      },
      "acceptanceCriteria": [
        "In the accepted view, the row of five bouncing emoji spans still renders as five separate spans with the same existing className values as before.",
        "Each of those five spans displays exactly the string \"❤ 🥰 😏 💋💌\" as its content.",
        "No other text or UI outside these five spans is changed."
      ]
    }
  ],
  "constraints": [
    "Apply changes ONLY to the 5 selected span elements (the five emoji spans in the accepted view).",
    "Do not modify any files under frontend/src/components/ui or any immutable hook/bootstrap paths listed in SYSTEM_CONTEXT."
  ],
  "nonGoals": [
    "Changing any other copy (headline/body text) in the accepted/question/declined views.",
    "Changing animations, timing, layout, or styling beyond the text content of the five selected spans.",
    "Any backend changes."
  ],
  "imageRequirements": {
    "required": [],
    "edits": []
  }
}